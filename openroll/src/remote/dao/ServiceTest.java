/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package remote.dao;

//import com.perp.proxy.Fingerprints;
//import com.perp.proxy.GetDetailVoterInformationResponse;
//import com.perp.proxy.LoginServiceResponse;
//import com.perp.proxy.VerifyServiceResponse;

/**
 *
 * @author Maverick
 */
public class ServiceTest {
/*
    public static void main(String[] args) {

        LoginServiceResponse lr = null;

        for (int i = 0; i < 1; i++) {
            lr = PartnerServiceManager.getLogin("general", "general");

            if (lr.getOperationResult().isSuccess()) {
                System.out.println(i + " token =" + lr.getSessionUuid());
            } else {

                System.out.println(i + "error =" + lr.getOperationResult().getError().getErrorMessage());

            }

        }

        VerifyServiceResponse vr = new VerifyServiceResponse();

        Fingerprints fp = new Fingerprints();
        vr = PartnerServiceManager.getVerification("19861592830128185", "1986/03/02", fp);

        if (vr.getOperationResult().isSuccess()) {
            System.out.println(9 + " father name =" + vr.getVoterInfo().getFather());
        } else {
            System.out.println(9 + "error =" + vr.getOperationResult().getError().getErrorMessage());
        }

        GetDetailVoterInformationResponse dv=new GetDetailVoterInformationResponse();
        dv = PartnerServiceManager.getInformationWithoutFP("19861592830128185", "1986/03/02");

        if (dv.getOperationResult().isSuccess()) {
            System.out.println(9 + " my name =" + dv.getNameEn());
            System.out.println(9 + " present address =" + dv.getPresentAddress());
            System.out.println(9 + " perm address =" + dv.getPermanentAddress());
            
        } else {
            System.out.println(9 + "error =" + dv.getOperationResult().getError().getErrorMessage());
        }



    }
    */ 
}
